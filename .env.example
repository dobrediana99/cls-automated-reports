# OIDC auth for POST /run/weekly and /run/monthly (Cloud Scheduler sends Bearer ID token).
# Set to your Cloud Run service URL (no path), e.g. https://my-service-xxx.run.app
OIDC_AUDIENCE=
# Optional: restrict callers to this service account email (token payload.email must match).
# SCHEDULER_SA_EMAIL=

# Monday.com API
MONDAY_API_TOKEN=
# Throttling and retry (enterprise-grade). Monthly/weekly can produce many calls; throttle+retry avoids 429.
# MONDAY_MAX_CONCURRENT=2
# MONDAY_MIN_DELAY_MS=200
# MONDAY_TIMEOUT_MS=60000
# MONDAY_MAX_ATTEMPTS=8
# MONDAY_RETRY_BASE_MS=800
# MONDAY_RETRY_MAX_MS=30000

# Vertex AI (Gemini) for monthly report analysis – IAM auth on Cloud Run (no API key).
# Cloud Run provides GOOGLE_CLOUD_PROJECT automatically.
# GOOGLE_CLOUD_PROJECT=
# GCLOUD_PROJECT=   # alternative to GOOGLE_CLOUD_PROJECT
# Gemini model (default: gemini-1.5-pro)
# GEMINI_MODEL=gemini-1.5-pro
# Vertex AI region (default: europe-west1)
# VERTEX_LOCATION=europe-west1

# Gmail SMTP for weekly/monthly email send (required when DRY_RUN is not set)
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Optional: set to 1 to write artifacts to out/ instead of sending email
# DRY_RUN=1

# Send mode: test (default) or prod. Only applies when DRY_RUN is not set.
# SEND_MODE=test
# In test mode, ALL emails go only to TEST_EMAILS (comma-separated). Required when SEND_MODE=test.
# TEST_EMAILS=you@example.com,other@example.com
